# 万物起源 GENESIS
## 微信小游戏开发流程文档
**版本：v2.0 · 2026年4月**
**配合设计文档 GENESIS_GDD_v2.0.md 使用**

---

## 技术决策总览

| 项目 | 决策 | 原因 |
|------|------|------|
| 技术框架 | 原生 Canvas API | AI 对原生 JS 理解最深，代码透明 |
| 开发编辑器 | Cursor | 内置 AI 辅助，vibe coding 首选 |
| AI 工具 | Claude Code | 处理复杂多文件操作、生成项目框架 |
| 预览调试 | 微信开发者工具 | 官方工具，真机预览必须 |
| UI 设计 | Claude Design + Figma | Claude Design 生成 High Fidelity 原型，Figma 精修 |
| 版本管理 | Git + GitHub（Private） | 代码回滚和版本控制 |
| 开发语言 | JavaScript | 微信小游戏原生语言 |

---

## 目录

1. [已完成的开发阶段](#已完成的开发阶段)
2. [当前项目结构](#当前项目结构)
3. [核心开发经验](#核心开发经验)
4. [后续开发任务](#后续开发任务)
5. [Claude Code 提示词最佳实践](#claude-code-提示词最佳实践)
6. [Claude Design 使用指南](#claude-design-使用指南)
7. [Git 工作流](#git-工作流)
8. [真机测试清单](#真机测试清单)
9. [发布上线](#发布上线)

---

## 已完成的开发阶段

### 阶段 1：环境搭建 ✅
- Node.js, Cursor, Claude Code, 微信开发者工具, Figma 安装完毕
- macOS 环境

### 阶段 2：项目创建 ✅
- Claude Code 生成项目框架
- 棋盘显示 + 三层旋转
- GitHub 私有仓库 `yummyooyummy/genesis-game`

### 阶段 3：核心玩法 ✅
- 分裂、合成、吸附、combo 全部跑通
- 相邻判定（同圈固定 + 跨圈动态）
- 两步点击交互 + 连接线提示
- 合成-吸附流程放慢至 2 秒

### 阶段 4：分裂系统 ✅
- 合成后分裂（阶段性圈层偏向）
- 定时自动分裂（动态间隔，无安全期）
- 分裂前摇（核心内部脉冲，不超出核心范围）
- 开局流程优化（自动吸附→Lv.2→玩家操作）

### 阶段 5：道具系统 ✅
- 3 种道具：清空、升级、暂停
- 获得方式：Combo ≥ 2 掉落 + 核心 Lv.5+ 升级赠送
- 掉落物悬浮 + 拾取 + 道具栏 UI
- 道具使用效果

### 阶段 6：计分优化 ✅
- 基础分 ×5（原 ×10）
- Combo 递增更平缓
- 吸附奖励 ×20（原 ×100）

### 阶段 7：UI 设计（进行中）
- Claude Design 生成 High Fidelity 原型（冷色调宇宙风格）
- 游戏主界面、开始界面、暂停弹窗、结束界面、目标达成横幅
- 道具按钮三种状态规范
- UI 参数配置化（UI_CONFIG）进行中

### 阶段 8：存档 + 目标系统（待实现）
- playerData.js 本地存档模块
- 单局动态目标机制
- 游戏结束界面（含破纪录特效）

---

## 当前项目结构

```
genesis-game/
├── game.js              ← 游戏入口和主循环
├── game.json
├── project.config.json  ← 含 AppID
├── GENESIS_GDD_v2.0.md
├── GENESIS_DEV_WORKFLOW_v2.0.md
├── design/
│   └── ui_prototype.png ← 原型图参考
├── js/
│   ├── board.js         ← 棋盘数据 + 分裂逻辑
│   ├── renderer.js      ← Canvas 渲染（UI_CONFIG 参数化）
│   ├── particles.js     ← 粒子效果
│   ├── input.js         ← 触摸事件（两步点击）
│   ├── score.js         ← 计分
│   ├── items.js         ← 道具系统
│   ├── playerData.js    ← 本地存档（待实现）
│   └── config.js        ← 统一配置参数
└── images/
```

---

## 核心开发经验

> ⚠️ 以下是实际开发中踩过的坑，**非常重要**，必须遵守。

### 经验 1：一次只改一项，每次 git commit

**血泪教训：** 曾经一次性改了 5 项优化（分裂间隔 + 分数 + Game Over + 合成流程 + 分裂位置），结果引入严重 bug（第一次合成后游戏卡住），修了很久修不好，最终回滚重做。

**正确做法：**
```
修改一项 → 测试确认 → git commit → 再改下一项
```

### 经验 2：Claude Code 需要精确的数值规范

**问题：** 给 Claude Code 说"道具栏应该是灰色大圆盘"，它按自己的理解做出来和原型图差距很大。

**正确做法：** 给精确的像素、颜色代码、比例数值。如果不确定数值，先让 Claude Code 把参数提取到 UI_CONFIG 对象中，然后自己手动调。

### 经验 3：让 Claude Code 先说计划再动手

**问题：** 让 Claude Code 直接改代码，改完发现方向不对，回退代价大。

**正确做法：** 每次发提示词都加：
```
修改前先告诉我：
1. 你打算改哪些文件、哪些函数
2. 具体的代码逻辑
我确认后再改。
```

### 经验 4：原型图放到项目里供 Claude Code 参考

```bash
mkdir -p ~/genesis-game/design
# 把原型图命名为 ui_prototype.png 放入
```

### 经验 5：UI 调整最有效的方法是"参数配置化"

与其反复让 Claude Code 调整视觉效果（它理解不了你想要什么），不如让它把所有视觉参数提取到 UI_CONFIG 对象顶部，你自己在代码里改数字 → 微信开发者工具即时预览。

---

## 后续开发任务

### 任务清单（按优先级排序）

| 优先级 | 任务 | 状态 | 依赖 |
|--------|------|------|------|
| P0 | UI 1:1 还原（参数配置化 → 手动微调） | 进行中 | - |
| P1 | 本地存档系统（playerData.js） | 待做 | - |
| P2 | 游戏结束界面（含破纪录特效） | 待做 | P1 |
| P3 | 开始界面（含成就区 + 进度条） | 待做 | P1 |
| P4 | 暂停弹窗（暂停旋转+计时器） | 待做 | - |
| P5 | 单局目标系统（动态目标+达成横幅） | 待做 | P1 |
| P6 | 完整页面流转（开始→游戏→结束→开始） | 待做 | P2+P3+P4 |
| P7 | Game Over bug 排查修复 | 待做 | - |

### 每个任务的实现步骤

**P1 本地存档：**
1. 创建 js/playerData.js
2. 实现 loadPlayerData / savePlayerData / updateAfterGame / getCurrentGoal
3. 在 game.js 启动时调用 loadPlayerData
4. 在游戏结束时调用 updateAfterGame
5. 测试：玩一局，检查存档是否正确保存

**P2 结束界面：**
1. 新增 gameState: 'playing' | 'gameOver'
2. Canvas 绘制结束界面（毛玻璃遮罩 + 信息面板）
3. 按钮触摸检测（重新开始 / 返回主页）
4. 破纪录特效（金色+NEW!+粒子）
5. 测试：玩到 Game Over 检查界面

**P3 开始界面：**
1. 新增 gameState: 'menu'
2. Canvas 绘制开始界面（星球 + 标题 + 成就区）
3. 读取 playerData 显示成就数据
4. 开始按钮触摸检测
5. 测试：重启游戏检查开始界面

---

## Claude Code 提示词最佳实践

### 模板 1：单项功能修改

```
需要修改 [具体功能]。

【问题描述】
[描述当前问题]

【期望效果】
[精确描述期望的结果]

【实现要求】
1. 只改 [文件名] 中的 [函数名]
2. 修改前先告诉我你的计划
3. 不要改动其他文件

【配置参数】（如果涉及数值）
[列出具体的数值]
```

### 模板 2：bug 排查

```
发现了一个 bug：[描述现象]。

请排查以下可能原因：
1. [可能原因 1]
2. [可能原因 2]
3. [可能原因 3]

排查清楚后告诉我原因，我确认后再修复。
```

### 模板 3：UI 参数化

```
请把以下 UI 参数提取到顶部的 UI_CONFIG 对象中。
不要改任何视觉效果，只改代码结构。

UI_CONFIG = {
  [参数名]: [当前值],  // [说明]
  ...
}
```

---

## Claude Design 使用指南

### 入口
- URL：claude.ai/design
- 需要 Pro / Max / Team / Enterprise 计划

### 工作流

1. **创建项目：** 选 Prototype → High fidelity → Create
2. **上传原型图**并描述需求
3. **迭代优化：** 在对话中说"把这个按钮颜色改成..."
4. **导出：** 导出 HTML/CSS 或使用 Claude Code 交接功能

### 本项目已用 Claude Design 完成的设计

- 游戏主界面（3 个变体：正常/达成横幅/暂停弹窗）
- 开始界面（2 个变体：回归玩家/新玩家）
- 游戏结束界面（3 个变体：正常/破纪录/未达成）
- 道具按钮规范（3 种道具 × 3 种状态）

### 设计到代码的流程

```
Claude Design 生成原型
  → 从原型中提取精确的 CSS 数值（颜色、字体、间距）
  → 写入 Claude Code 的提示词
  → Claude Code 按数值实现
  → 手动微调不满意的部分
```

---

## Git 工作流

### 日常开发

```bash
# 改代码前先确认状态
git status

# 如果有未提交的修改，先提交
git add -A
git commit -m "feat: [功能描述]"

# 改完一项功能后立即提交
git add -A
git commit -m "feat: [本次修改的描述]"

# 推送到远程
git push origin main
```

### 出问题时回滚

```bash
# 查看最近的 commit
git log --oneline -10

# 回滚到某个 commit
git reset --hard <commit-hash>
```

### commit 消息规范

```
feat: 新功能         例：feat: 道具系统完整实现
fix: bug 修复        例：fix: Game Over 判断条件修正
style: UI 调整       例：style: 道具栏颜色调整为灰色
refactor: 代码重构   例：refactor: 提取 UI_CONFIG 参数
```

---

## 真机测试清单

### 每次大改后必须验证

- [ ] 触摸响应准确（两步点击 + 道具栏 + 按钮）
- [ ] 旋转流畅（不卡顿）
- [ ] 合成-吸附流程 2 秒节奏正确
- [ ] 定时分裂按计划触发
- [ ] 前摇特效不超出核心范围
- [ ] 道具掉落 + 拾取 + 使用正常
- [ ] 暂停道具计时器暂停/恢复正常
- [ ] 分数计算正确（一局约 2000-4000）
- [ ] Game Over 在 37 格满时正确触发
- [ ] 连接线实时跟随旋转变化
- [ ] 道具栏可用/不可用态区分清晰
- [ ] 格子大小在小屏手机上也能点到
- [ ] 长时间游戏（10分钟+）无内存泄漏

### 存档功能验证（P1 完成后）

- [ ] 游戏结束后存档更新
- [ ] 重新打开游戏后读取存档
- [ ] 最高分正确记录
- [ ] 最高等级正确记录
- [ ] 清除存档后恢复为初始值

---

## 发布上线

### 审核材料

| 材料 | 说明 | 状态 |
|------|------|------|
| 软件著作权证书 | 上线前置条件 | **尽早申请** |
| 游戏截图 | 至少 3 张 | 开发完成后截图 |
| 游戏介绍文案 | 简短描述 | 待准备 |
| 游戏类目 | 休闲益智 | 注册时选择 |

### 上传流程

1. 微信开发者工具 → 上传 → 填写版本号
2. mp.weixin.qq.com → 版本管理 → 提交审核
3. 审核通过（1-3 工作日）→ 发布

---

*文档版本：v2.0 · 2026年4月*
*配合 GENESIS_GDD_v2.0.md 使用*